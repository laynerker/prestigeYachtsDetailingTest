{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Next.js: depuración",
            "type": "node-terminal",
            "request": "launch",
            "command": "npm run dev",
            "serverReadyAction": {
                "action": "debugWithEdge", // o "debugWithChrome"
                "pattern": "- Local: +(https?://localhost:([0-9]+))",
                "uriFormat": "%s"
            }
        }
    ]
}